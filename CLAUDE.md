# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## プロジェクトの目的

このリポジトリは、Notion のコンテンツを Twitter/X に自動投稿するためのツールです。初回コミット時点ではソースファイル・依存関係・設定ファイルがまだ存在せず、README.md も空の状態です。

## 現在の状態

このプロジェクトは実装前の段階です。開発を始める前に、以下の基本事項を決定してこのファイルに記録してください。

- **言語 / ランタイム** — 例: Python、Node.js/TypeScript
- **パッケージマネージャー** — 例: pip/poetry、npm/pnpm/yarn
- **主要な依存ライブラリ** — Notion API クライアント、Twitter/X API クライアント、スケジューリング機能
- **エントリーポイント** — 自動化をどのようにトリガーするか（cron、GitHub Actions、常駐プロセスなど）
- **設定管理** — API 認証情報や設定の供給方法（`.env`、環境変数、シークレットマネージャーなど）

## コードが追加されたとき

以下の内容をこのファイルに追記してください。

1. **ビルド / インストールコマンド** — 依存関係のインストールと環境構築の手順
2. **実行コマンド** — メインの自動化処理および補助スクリプトの起動方法
3. **テストコマンド** — テストスイート全体および個別テストの実行方法
4. **リント / フォーマットコマンド** — リンターとフォーマッターの実行方法
5. **アーキテクチャの概要** — Notion → 処理 → Twitter/X 投稿までのデータフロー（キューイング、重複排除、スケジューリングロジックを含む）
6. **環境変数** — 必要な環境変数の名前と用途（値は含めない）
